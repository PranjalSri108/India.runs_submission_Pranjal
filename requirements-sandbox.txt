# Sandbox/demo only (app.py). NOT needed to produce submission.csv — the ranker
# itself is pure Python stdlib (see requirements.txt). Kept separate so the
# Stage-3 ranking reproduction stays dependency-free.
#
# This is the complete, pinned dependency set the hosted sandbox needs. Deploy a
# Streamlit Cloud / HF Space from these three lines (see README "Deploying the
# sandbox"). pandas is a transitive dep of streamlit but app.py imports it
# directly, so it is pinned explicitly rather than left to resolution.
streamlit==1.38.0
altair==5.5.0
pandas==2.3.3
